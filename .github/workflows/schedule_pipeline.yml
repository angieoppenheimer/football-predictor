name: Execute Predictive Analytics Pipeline

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:

        ref: ${{ github.head_ref || github.ref_name }}

    - name: Setup Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests pandas openpyxl

    - name: Run Predictive Analytics Engine
      env:
        API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
      run: python main.py

    - name: Commit and Push Updates
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        
        git checkout ${{ github.head_ref || github.ref_name }}
        git pull origin ${{ github.head_ref || github.ref_name }} --rebase
        
        git add data/ README.md

        git diff-index --quiet HEAD || git commit -m "Data & README Auto-Update: $(date +'%Y-%m-%d')"
        git push origin HEAD:${{ github.head_ref || github.ref_name }}
